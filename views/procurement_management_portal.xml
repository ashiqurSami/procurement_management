<odoo>
    <data>
        <!--     add menu in the portal-->
        <template id="portal_supplier_home" inherit_id="portal.portal_my_home">
            <xpath expr="//div[@id='portal_client_category']" position="inside">
                <t t-call="portal.portal_docs_entry">
                    <t t-set="icon" t-value="'/sale/static/src/img/bag.svg'"/>
                    <t t-set="title">Supplier</t>
                    <t t-set="text">Supplier Dashboard</t>
                    <t t-set="url" t-value="'/procurement_management/sign-up'"/>
                    <t t-set="config_card" t-value="True"/>
                </t>
            </xpath>
        </template>

        <!--    sign up template-->
        <template id="sign_up_template" name="Sign Up Page">
            <t t-call="portal.portal_layout">
                <div class="container d-flex justify-content-center align-items-center vh-100">
                    <div class="card shadow-lg p-4 rounded-lg"
                         style="max-width: 400px; width: 100%; border-radius: 12px;">
                        <div class="text-center mb-4">
                            <img src="/your_module/static/img/logo.png" alt="Company Logo"
                                 style="max-width: 100px;"/>
                            <h3 class="mt-3" style="font-weight: 600; color: #333;">Sign Up</h3>
                            <p style="color: #777;">Enter your email to receive an OTP</p>
                        </div>
                        <form action="/procurement_management/otp" method="post">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="form-group">
                                <input type="email" name="email" required="required" class="form-control"
                                       placeholder="Enter your email"/>
                            </div>
                            <button type="submit" class="btn btn-primary mt-3">Submit</button>
                        </form>
                    </div>
                </div>
            </t>
        </template>

        <template id="otp_verify_template" name="OTP Input Page">
            <t t-call="portal.portal_layout">
                <t t-set="title">Enter OTP</t>
                <div class="container d-flex justify-content-center align-items-center vh-100">
                    <div class="card shadow p-4 text-center" style="max-width: 400px; width: 100%;">
                        <h2 class="mb-3">Enter OTP</h2>
                        <p class="text-muted">We've sent a one-time password to your email. Enter it below to verify.</p>

                        <form action="/procurement_management/verify" method="post">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <input type="hidden" name="email" t-att-value="email"/>

                            <div class="mb-3">
                                <input type="text" name="otp" class="form-control text-center fw-bold fs-4"
                                       placeholder="Enter OTP" maxlength="6" />
                            </div>

                            <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
                        </form>

                        <p class="mt-3 text-muted">Didn’t receive the OTP?
<!--                           <a t-att-href="'/procurement_management/otp?email=' + email" class="fw-bold">Resend</a>-->
                            <a href="#" class="fw-bold">Resend</a>
                        </p>
                    </div>
                </div>
            </t>
        </template>

    </data>
</odoo>



